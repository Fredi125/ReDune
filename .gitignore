# Reference repos (cloned for cross-referencing, not project code)
refs/

# Python
__pycache__/
*.pyc
*.pyo

# Temp/output
/tmp/
*.bak

# Game archive (too large for git)
DUNE.DAT

# Voice file subdirectories (CD audio data)
gamedata/P*/
DUNE_NEW.DAT
REPACKED.DAT

# Web app build artifacts / deps (see web/.gitignore for the rest)
web/node_modules/
web/dist/

# Bulk asset-export output (tools/extract_all.py) — derived game data, never committed
extracted/
*.ppm
